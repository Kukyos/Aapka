# local AI-assistant config, not part of the project
CLAUDE.md
.claude/
graphify-out/

# secrets
.env

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# runtime data — patient data must never be committed
server/aapka.db
server/bundles/
eval/documents/
eval/noise/

# node
node_modules/
dist/
*.log

# os
.DS_Store
Thumbs.db

# build artefacts
*.tsbuildinfo

# Draft submission material. Local only.
submission/

# Real patient documents. Prescriptions, lab reports, discharge summaries and the
# photographs of them used to test the OCR pipeline. These are somebody's medical
# records: they stay on the machine that needs them and never enter a public repo,
# whoever they belong to and however anonymous they look.
server/eval/documents/
server/eval/noise/
*.prescription.*
